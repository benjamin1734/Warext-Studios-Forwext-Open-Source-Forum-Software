# tests

Cross-cutting automated test suites and test infrastructure.

The final test layout covers unit, integration, API, browser/E2E, security/permission, migration/install/update and module/add-on lifecycle behavior. Domain-adjacent tests may be organized near their subsystem only when the repository test standard explicitly permits it; all required suites remain discoverable from the central test tooling.
