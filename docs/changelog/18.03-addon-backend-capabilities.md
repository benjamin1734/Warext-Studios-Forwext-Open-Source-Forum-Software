# 18.03 — Add-on backend capabilities

Status: **completed**

Version: `0.0.7.53-dev`

## Delivered

- Typed canonical backend registration for migrations, entities, routes, permissions, settings, ACP items, jobs/cron, search, notifications, webhook metadata and content types.
- Shared migration provisioning plus persisted typed add-on settings and synchronized permission catalog entries.
- Existing router/queue/scheduler/search/notification registry integration with atomic preflight.
- Owner-aware entity/webhook/content-type metadata registry.
- Lifecycle-aware activation restricted to persisted enabled add-ons.
- Fail-closed scheduled-task → queue-job consistency validation.
- Backend permission and Administration audit enforcement for add-on setting mutations.
- Extension-aware ACP navigation boundaries without replacing backend authorization.
- Regression coverage for namespace ownership, conflicts, inactive lifecycle states, settings, migrations and runtime composition.

## Verification

- Build/package workflow `36155742361`: passed.
- Database migration smoke `36155742170`: passed on MySQL 8.4 and MariaDB 10.11.
- No new mandatory cPanel runtime service/dependency was introduced.
- Webhook transport/delivery security remains intentionally deferred to the dedicated 19.03 webhook platform rather than being implemented as an unsafe placeholder.

## Completion pointer

Implementation/fix completion commit: `3c88318dfc937da2a16ffd0785fe51d4d1aa34f3`.

Next roadmap item: `18.04 — Add-on UI capabilities`.
