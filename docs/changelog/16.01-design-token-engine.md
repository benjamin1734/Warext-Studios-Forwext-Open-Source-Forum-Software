# 16.01 — Design Token Engine

- Added a versioned canonical design-token manifest covering color, typography, spacing, radius, border, shadow, motion and semantic tokens.
- Added strict PHP manifest parsing, token/category validation, reference integrity and cycle detection.
- Added CSS-injection-safe primitive value policies and semantic reference-only tokens.
- Added deterministic native CSS custom-property compilation with reduced-motion duration overrides.
- Integrated the native PHP frontend while preserving existing visual variables through semantic compatibility aliases.
- Added a TypeScript/React consumption boundary using the same canonical JSON manifest.
- Added regression coverage for category completeness, semantic resolution, unsafe-value rejection, cycles, compiler output and native/frontend sharing.
- No migration or new cPanel runtime dependency is required.
- GitHub Actions build run `35897236806` and database migration smoke run `35897236805` succeeded on the final 16.01 implementation head.
