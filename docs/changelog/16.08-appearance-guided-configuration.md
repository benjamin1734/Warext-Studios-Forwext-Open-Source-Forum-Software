# 16.08 — Basitlik Katmanı ve Guided Configuration

- Added read-only native PHP Appearance Studio at `/admin/appearance`.
- Added Basic/Advanced progressive disclosure without hidden state mutation.
- Added typed safe presets with explicit change summaries and allowlisted preview variables.
- Added bounded static-metadata search that does not inspect protected theme/layout values.
- Added reset-to-default for the guided state.
- Added isolated desktop/tablet/mobile preview with reduced-motion support and no JavaScript dependency.
- Added task-language setup assistant linking directly to the real theme and layout editors.
- Added backend `appearance.manage` enforcement and per-item `appearance.advanced` availability explanations.
- Kept live mutation, publish, rollback, CSRF and audit responsibilities in the existing revision-aware theme/layout services.
- Added service and native web-surface regression coverage.
- No database migration is required because the guide carries no durable configuration state.
- GitHub Actions build run `35911852262` and database migration smoke run `35911851992` succeeded on the final 16.08 implementation head.
