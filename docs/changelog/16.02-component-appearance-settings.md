# 16.02 — Component Appearance Settings

- Added typed appearance targets for header, navigation, footer, forum, thread, post, profile, buttons, inputs, modals, badges, role banners, editor, tables and alerts.
- Added a versioned component appearance manifest binding only validated appearance properties to existing design-token keys.
- Added registry validation for required targets, duplicate targets, property names and design-token references.
- Added deterministic component CSS-variable compilation with no raw selector or arbitrary CSS injection surface.
- Integrated current native PHP header/navigation/cards/profile/buttons/inputs/badges/alerts with the new variables.
- Integrated role-banner CSS with the shared component appearance contract.
- Exposed the same component manifest to the optional TypeScript/React package.
- Added component registry/compiler/web-surface regression coverage.
- No migration or new cPanel runtime dependency is required.
- GitHub Actions build run `35898405999` and database migration smoke run `35898406003` succeeded on the final 16.02 implementation head.
