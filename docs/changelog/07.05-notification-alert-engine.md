# 07.05 — Notification / Alert Engine

- Added a shared notification type registry and provider-neutral module/add-on API for first-party and future third-party producers.
- Added persisted in-app alerts plus email/push delivery rows with category/channel preferences and safe per-type defaults.
- Added notification grouping, producer-level deduplication and recipient-scoped transaction/`FOR UPDATE` serialization to prevent concurrent duplicate/group races.
- Added deterministic bounded template substitution, scalar payload validation and same-origin action-path enforcement to avoid template execution and open redirects.
- Added provider-neutral email/push transports, retryable delivery state, exception containment, machine-safe error codes and exponential backoff capped at eight attempts.
- Added owner-scoped inbox/read operations and shared permission-engine checks through `notification.alert.view` and `notification.preference.manage`.
- Added migration `20260917001000_notification_alerts` with four tables, five foreign keys, two permissions, ten starter-profile rules and installer-registry verification.
- Added migration/registry/dispatcher/delivery/repository tests plus architecture documentation; focused 07.05 validation passed 13 test methods, PHP 8.4 syntax checks and runtime smoke coverage.
- Feature commit: `6b955722f9c2c3766ca544decb613c08bdc4e393`.
- Concurrency hardening commit: `c9c70266c02e4a26b0329f288b465bdf3f439818`.

The root `CHANGELOG.md` predates several completed 06.x/07.x entries. This fragment preserves the complete 07.05 delivery record without deleting or rewriting that historical file; a later changelog normalization can merge accumulated fragments into the root changelog losslessly.
