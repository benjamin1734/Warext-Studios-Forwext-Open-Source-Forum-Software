# 16.03 — Background / Pattern / Gradient / Asset System

- Added typed solid, gradient, image and built-in pattern background modes.
- Added bounded opacity, scale, rotation and background blend-mode controls.
- Added animated-gradient support with deterministic keyframes and reduced-motion fallback.
- Added site, header, category and profile scopes with opaque-id validation for entity scopes.
- Added safe same-origin raster appearance asset paths with traversal/external-URL/SVG rejection.
- Added a versioned default background manifest for the native site and header.
- Added base-path-aware CSS compilation using isolated pseudo-layers so opacity/transforms do not affect content.
- Added native site/header scope markers and profile scope-id markup.
- Added shared TypeScript/React manifest exposure.
- Added domain, registry, compiler and web-surface regression coverage.
- No migration or new cPanel runtime dependency is required.
- GitHub Actions build run `35901010331` and database migration smoke run `35901010425` succeeded on the final 16.03 regression-corrected head.
