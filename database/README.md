# database

Database evolution resources.

This directory will hold versioned core/module/add-on migration registrations, schema-related fixtures and controlled seed/install data when the migration engine is implemented.

Production schema changes must flow through the migration/install/upgrade engine. Ad-hoc manual SQL is not the normal update path.
